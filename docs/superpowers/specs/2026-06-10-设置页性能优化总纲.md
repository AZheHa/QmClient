---
type: spec
date: 2026-06-10
status: active
confidence: high
supersedes:
  - docs/superpowers/explore/2026-06-02-设置页UI性能优化第一性原理分析.md
  - docs/superpowers/explore/2026-06-02-设置页性能量化系统设计.md
scope:
  - 设置页（Settings）全部 tab 的冷启动、切 tab、滚动性能
  - Tee 页皮肤列表的加载与交互性能
  - 性能量化系统的设计与实现
---

# 设置页性能优化总纲

## 速答

设置页卡顿的根因不是"缺 FBO"或"缺预热"，而是**每一帧做了不该做的工作**：不可见 section 的 widget 仍然执行、静态文本每帧重建、后台 job 结果集中合并且与交互帧重叠。

当前实际代码状况：
- **FBO 清理已完成**：设置页不再保留页面级、section 级 FBO / recorded render target / runtime cache 路径，后续优化不再依赖任何 FBO 命中或预热方案。
- **CSectionLoader** 仍是核心调度器：它具备四状态机（`UNINITIALIZED → MEASURING → COMPACT → FULL`）能力，但当前 live 调用大多显式 `SetProgressiveEnabled(false)`，因此实际行为更接近“先测量，再按可见性与预算直接走 full render 或跳过 full render”；视口外 section 仍会被测量高度。
- **资源 Jobs** 已有完整的吞吐量控制器，但当前文档对 `IDLE_DRAIN` 的归因写得过满：可见区皮肤仍然优先加载，`IDLE_DRAIN` 的后台请求预算 24 更准确地说是**历史请求积压的放大器**。在一次从列表顶部快速拖到底部这类极端交互后，`BACKGROUND_REQUESTED` 可能累积到上千项，随后在主线程合并阶段暴露为 5575ms 级别的尖峰。
- **当前生效的设置页 runtime prewarm 入口** 是 `CMenus::PrewarmVisibleSettingsResources()`：它先走页面资源预热，再对 TClient / QmClient 页走 `PrewarmOnly` 渲染路径。预热仍有收益，但覆盖并不均匀，且不同页面的预热路径与首帧实际路径仍需逐页用 telemetry 验证。
- **QmUI 当前分析仍偏弱**：现有文档不能再把 QmUI 简化成 `DrawCard` overdraw。当前代码里 `CUiRuntimeV2` 已经有 `tree_begin_frame`、`anim_advance`、`render_bridge_begin_frame`、`ui_runtime_total` 等 perf stage，但它们还没有和设置页操作路径、layout 成本、节点数量、动画数量做统一关联，因此 QmUI 仍缺少独立验收口径。

当前 baseline（2026-06-10，post-FBO-cleanup，1ms 采样阈值）：
- **verdict: FAIL**，**p99 = 12.906ms**，**spike 帧 = 464 个**
- 第一热点：`settings:tee / Work Drain` — 单帧合并 272 个 job 结果，耗时 **6839.642ms**
- 第二热点：`settings:tee / Work Drain` — 单帧合并 495 个 job 结果，耗时 **3901.435ms**
- 第四热点仍是 `server_browser / List Interaction` — 68.325ms（不在本轮范围）

这份 `2026-06-10` 数据是**当前 FBO 删除后唯一基线**。它反映的是设置页侧 FBO 清理已经落地后的真实运行状态；后续所有性能计划、实现优先级和回归判断，都应默认以这份数据为起点。它仍然属于**混合操作路径**，因此如果要做严格的 settings-only 验收，仍需在这份基线之上继续补固定场景 session。

核心量化目标：**设置页任意交互（切 tab、滚动、打开页面）的帧时间 ≤ 8.33ms**（120Hz），最差帧 p99 ≤ 16.67ms（60Hz）。

---

## 1. 问题定义

### 1.1 用户感知到的问题

玩家在游戏中打开设置页、切换 tab、滚动皮肤列表时，画面会明显卡一下甚至卡住几秒。这不是"画面不流畅"，而是**帧时间尖峰**——某一帧突然耗时几十甚至几百毫秒，超过了显示器刷新周期。

具体的用户场景：

- **打开设置页**：按 Escape → 设置，画面停顿 100-500ms 才出现界面
- **切 tab**：从 General 切到 Tee，画面停顿 50-200ms
- **滚动皮肤列表**：在 Tee 页滚动时，滚动不跟手，偶尔卡一下
- **静止不动**：什么都不做时帧时间 2-3ms，很流畅

### 1.2 为什么会卡 — 帧预算

DDNet 每一帧要做两件事：游戏渲染（世界、实体、特效、HUD）和菜单渲染（设置页、服务器列表等）。这两件事跑在同一个线程里，共享同一帧的时间预算。

```
一帧 16.67ms（60Hz）
├── 游戏渲染：8-12ms
├── 菜单渲染：2-4ms（正常）
└── 剩余：给下一帧
```

当菜单渲染突然从 2ms 涨到 200ms，这一帧就"爆了"——游戏画面停顿，玩家感受到卡顿。

优化的目标不是让菜单渲染"更快"，而是让它**不要突然变慢**——消灭尖峰帧，保持帧时间稳定。

### 1.3 范围边界

**本轮只做设置页**。具体范围：

- 设置页全部 tab：General、Tee、Appearance、Controls、Graphics、Sound、Assets、DDNet、TClient、QmClient
- Tee 页的皮肤列表加载与滚动
- 性能量化系统（采集 + 分析 + 报表）

**明确不做**（延后到有证据时再考虑）：

- Server Browser 的列表交互优化（当前遍历 1281 行只渲染 24 行的效率问题）
- Demo Browser 的目录扫描异步化
- Assets / Workshop 的进一步优化
- 全局 scheduler 大重构
- 任何非设置页的页面

**为什么不做 Server Browser？** 虽然 perf 数据显示它有 78ms 的列表交互帧，但这是不同根因的问题（同步遍历 vs 资源合并），不应和设置页混在一起优化。设置页的问题是资源 drain + 每帧重建，Server Browser 的问题是列表全量遍历。

---

## 2. 量化目标

### 2.1 帧时间分层目标

DDNet 是快节奏平台游戏，流畅性要求高于一般应用。在设置页上下文中：

| 指标 | 目标值 | 理由 |
|------|--------|------|
| UI 帧时间均值 | ≤ 4ms | 为游戏渲染留足 12ms+ 预算（60fps） |
| 交互帧（切 tab/滚动） | ≤ 8ms | 不超过 120Hz 刷新周期 |
| 最差帧 (p99) | ≤ 16ms | 不超过 60Hz 刷新周期；超出即用户可感知卡顿 |
| 首帧（冷启动进入设置页） | ≤ 33ms | 允许一次较长帧，但不能连续两帧超标 |

**关键洞察**：帧时间的 **p99** 比平均值重要得多。一次 50ms 的卡顿远比 10 次 5ms 的微波动更影响体验。优化应优先消灭"尖峰帧"。

### 2.2 无上限 FPS 指标

无上限 FPS 是设置页性能最直观的端到端指标。它不解释瓶颈来自哪里，但它直接回答"用户最终感受到的吞吐量有没有变好"。

| 指标 | 用途 | 解读 |
|------|------|------|
| `avg FPS` | 观察整体吞吐量 | 同路径优化后 avg 上升，通常说明总 CPU/GPU 成本下降 |
| `min FPS` | 捕捉最差体感和尖峰 | min 下降或长期偏低，优先回查 p99、长帧 timeline |
| `max FPS` | 观察稳态上限 | 只作辅助，易受计数窗口、后台负载、驱动限帧影响 |

采集时必须固定：操作路径、硬件、renderer、分辨率、UI scale、客户端版本、无上限帧率状态。FPS 只能和同一机器同一路径的 baseline 比较，不能跨机器比较绝对值。

### 2.3 量化验收标准

| 验收项 | 指标 | 测量方式 |
|--------|------|----------|
| 打开设置页首次渲染 | ≤ 1 帧 > 33ms, 后续帧 ≤ 16ms | `qm_perf_debug` 日志 |
| 切 tab（如 General → Tee） | ≤ 8ms | `qm_perf_debug` 日志 |
| 子 tab 切换 | ≤ 8ms | `qm_perf_debug` 日志 |
| 滚动一屏内 | 每帧 ≤ 8ms | `qm_perf_debug` 日志 |
| 滚动中快速翻页 | p99 ≤ 16ms | `qm_perf_debug` 日志 |
| 打开皮肤列表（100+ 皮肤） | 首帧 ≤ 16ms, 后续 ≤ 8ms | `qm_perf_debug` 日志 |
| 无上限设置页 FPS | 同路径 `avg/min FPS` 不低于 baseline | 客户端 FPS 统计 + perf report |

---

## 3. 实际代码现状

> 本章描述的是代码仓库中的实际实现，不是设计意图。所有结论基于当前分支 `codex/axiom-script-governance` 的代码。

### 3.1 设置页渲染路径

设置页每一帧的调用链：

```
CMenus::Render()                          # menus.cpp:2341
  └─ RenderSettings(MainView)             # menus_settings.cpp:4352
       ├─ 绘制左侧标签栏（10 个 tab）
       ├─ 检测当前选中的 tab
       └─ 对选中 tab 的内容区域：
            ├─ [FBO 缓存路径 — 正在删除] DrawSettingsPageRuntimeCache()
            ├─ [FBO 预热路径 — 正在删除] PrewarmSettingsSectionRuntimeCache()
            └─ [实际渲染路径] RenderSettingsXxx(ContentView)
                 ├─ CScrollRegion::Begin/AddRect/End    # 滚动区域
                 ├─ CUi::DoButton_Menu/DoLabel/DoEditBox # CUI widget
                 └─ ui_widget::DrawCard/PrimaryButton    # QmUI widget
```

**核心调度器：CSectionLoader**（`src/game/client/components/section_loader.cpp`）

CSectionLoader 用一个四状态机管理每个 section 的生命周期：

| 状态 | 动作 | 说明 |
|------|------|------|
| `UNINITIALIZED` | 调用 `MeasureFn` 测量高度 → 进入 `MEASURING` | 每个 section 的起始状态 |
| `MEASURING` | 计算视口优先级；如果靠近视口则渲染紧凑版 → `COMPACT` | 预取边距 200px |
| `COMPACT` | 如果有预算（每帧 5ms，最多解锁 2 个）且优先级高 → `FULL` | 预算用完后保持在 COMPACT |
| `FULL` | 如果脏了则重新测量，调用 `RenderFullFn` 完整渲染 | 真正的"用户看到的渲染" |

**当前 live 调用方式**：设置页中的主要调用点会显式 `SetProgressiveEnabled(false)`。这意味着 section loader 的通用状态机仍然存在，但 live path 更接近：

- 先对 section 做高度测量
- 当前帧根据视口优先级与预算决定是否走 `RenderFullFn`
- 视口外 section 通常不会走 full render，但仍然会被测量高度

**关键问题**：视口外 section 虽然不一定会渲染完整 widget，但仍然会被测量；这意味着“不可见 section 仍做 CPU 工作”的根因仍然存在。

预热函数 `Warmup()`（`section_loader.cpp:268-338`）走查所有 section：对靠近视口的调 `RenderCompactFn`（轻量渲染），对远处的只调 `MeasureFn`（测量高度）。如果某个 section 超过 1ms 就提前中断。

**问题**：预热收益并不稳定。部分页面/section 的预热路径与首帧真实展示路径并不完全一致，导致“预热做了工作，但首帧仍有冷启动成本”。这不是一个统一结论，后续需要逐页用 telemetry 验证。

### 3.2 FBO：已完成清理

FBO（Frame Buffer Object，帧缓冲对象）曾被用来做 section 的静态层缓存：把 section 的不变部分（背景、标题、分隔线）预先渲染到一张纹理，后续帧直接贴纹理，只重绘交互部分。

**当前状态：设置页侧 FBO 清理已完成，代码已可编译并通过当前 quick gate 与相关测试。**

已完成清理的部分：
- `section_loader.h/cpp`：FBO 相关的渲染目标、缓存有效性、静态/交互层拆分全部剥离，`PrewarmStaticRenderTargets`、`DrawCachedSectionByName`、`RecordStaticRenderTarget` 等函数已移除
- `settings_runtime_cache.h/cpp`：FBO 枚举值、渲染目标注册表、section 注册表、预热计划全部移除
- `settings_warmup.h/cpp`：`RUNTIME_FBO` 预热类型移除
- `menus.h`：所有 FBO 缓存数组（`m_aSettingsPageRuntimeCaches[32]` 等）、FBO 函数声明全部移除
- `menus.cpp`：`DestroySettingsPageRuntimeCaches` 等 FBO 函数实现移除
- `settings_resource_jobs.cpp`：`SettingsPageCanUsePageFbo` 等 FBO 相关函数移除
- `config_variables_qmclient.h`：`qm_settings_fbo_cache`、`qm_settings_fbo_cache_debug` 已移除
- `settings_warmup_test.cpp` / `section_loader_test.cpp`：旧 FBO 测试用例移除，新增 cleanup contract tests，验证源码不再引用旧 FBO 合同

**文档立场**：FBO 删除是本仓库已经落地的方向。本文档后续所有优化都不依赖任何 FBO 路径。

### 3.3 资源 Jobs 系统

**文件**：`src/game/client/components/settings_resource_jobs.h` / `.cpp`

这个系统负责皮肤纹理的异步加载和主线程合并。核心是一个 PID 风格的吞吐量控制器，根据当前帧的状态自动调整预算。

**四种运行模式**（`ESettingsSkinThroughputControllerMode`）：

| 模式 | 触发条件 | 后台请求预算 | 说明 |
|------|----------|-------------|------|
| `SCROLL_ACTIVE` | 用户正在滚动 | 0 | 交互优先，不发后台请求 |
| `POST_SCROLL_RECOVERY` | 刚停止滚动 | 0 | 恢复期，仍不发后台请求 |
| `IDLE_VISIBLE` | 空闲但可见区未稳定 | 可变 | 温和推进 |
| `IDLE_DRAIN` | 空闲且可见区已稳定 | **24**（偏大） | 放大历史后台请求积压 |

**更准确的说法**：`IDLE_DRAIN` 的预算 24 不是“可见项被后台饿死”的根因。当前实现里，可见区皮肤仍然先于后台项被调度；真正的问题是，当用户做过一次大跨度滚动后，系统已经留下了大量历史 `BACKGROUND_REQUESTED`。此时如果页面进入 `IDLE_DRAIN`，每帧最多 24 个后台请求会继续放大这份积压，最终在某一帧集中合并。perf 数据里那次 5575ms 尖峰对应的是单帧 merge 493 个 job，且当时 telemetry 记录了 `requested=1121`。

**背景请求预算决策**（`SettingsSkinBackgroundRequestBudgetDecision`）：
```
backlog_high_watermark = max(visible_reserve * 8, count_fuse_limit * 2)
available = count_fuse_limit - real_inflight
```

当前逻辑在"健康进展"（有加载完成）时不会限制新的后台请求——只要还有东西在加载，就可以继续往队列里塞。这导致了 backlog 的无限膨胀。

### 3.4 皮肤加载链路

**文件**：`src/game/client/components/skins.h` / `skins.cpp`

每个皮肤有 7 个状态：

```
UNLOADED → BACKGROUND_REQUESTED → PENDING → LOADING → LOADED
                                                   → ERROR
                                                   → NOT_FOUND
```

在设置页上下文中，加载分为三层优先级：

1. **可见区（VISIBLE）**：立即升级到 `PENDING`，不受延迟限制
2. **预取区（PREFETCH）**：需要等待 100ms 后才能进入 `PENDING`（防止快速滚动时抖动）
3. **后台（BACKGROUND）**：由吞吐量控制器的背景请求预算控制

每帧处理流程（`UpdateStartLoading`）：
- 将 `PENDING` 转为 `LOADING`（启动作业），遵守 `CountFuseLimit`（最大飞行中负载）和 `VisibleReserve`（为可见区保留的槽位）
- `UpdateFinishLoading` 完成作业（纹理上传），限制每帧最多 12 个，且在 GPU 预算内

### 3.5 预热系统

**当前 runtime 入口**：`src/game/client/components/menus.cpp` 中的 `CMenus::PrewarmVisibleSettingsResources()`

当前设置页 idle prewarm 的实际流程是：

1. `PrewarmVisibleSettingsResources()` 根据当前页面计算 content view
2. 先调用 `PrewarmSettingsPageResources(Page, Tab, ContentView)` 进行页面资源预热
3. 对 TClient / QmClient 页，再分别走 `RenderSettingsTClient(..., true)` / `RenderSettingsQmClient(..., true)` 的 `PrewarmOnly` 路径

这意味着当前真正生效的是“页面资源预热 + 页面自定义的 PrewarmOnly 渲染路径”。

**补充说明**：`CSettingsWarmupScheduler`（`src/game/client/components/settings_warmup.h/.cpp`）仍保留在仓库中，也有单测覆盖，但它不是当前设置页 runtime prewarm 的主入口，不应再把它当成现网设置页预热链路本身。

**当前问题**：预热不是完全无效，而是收益不稳定、覆盖不均。不同页面中 `RenderCompactFn`、`RenderFullFn` 和真实首帧路径的一致性不同，需要逐页用 telemetry 验证哪些预热真正有效。

---

## 4. 第一性原理：UI 性能优化的本质

### 4.1 一帧里到底发生了什么

```
Frame Time = CPU 提交 + GPU 绘制 + 同步等待
           = (布局计算 + 文本创建 + 输入检测 + 状态更新 + Draw Call 提交)
           + (顶点处理 + 光栅化 + 像素着色 + 混合)
           + (SwapBuffers / v-sync + 驱动同步)
```

优化每一帧本质上就是**减少上述每一项的工作量**，且**只在变化时才做**。

DDNet 的特性决定了很多"通用优化手段"不适用：
- DDNet 是 2D 俯视角游戏，所有渲染通过 quad + sprite + texture 完成
- 没有后处理链（bloom、motion blur、SSAO）
- UI 和游戏渲染共享同一个 GL/Vulkan context，共享帧预算
- 菜单渲染不是独立线程，发生在 `CMenus::OnRender()` 中

### 4.2 六个优化维度（按影响力排序）

#### 维度 1：少做 — 只处理可见和变化的部分

**原则**：不可见的 UI 元素不应消耗 CPU/GPU 时间。

**反模式**：每帧遍历所有 section/行/项，即使 90% 在屏幕外。

**DDNet 现状**：CSectionLoader 虽有视口优先级，但 MEASURING 阶段仍会测量所有 section 高度；列表渲染没有真正的虚拟化（只渲染可见行）。

**对应策略**：B（视口外 section 跳过 widget）、C（列表虚拟化）

#### 维度 2：少提交 — 减少 Draw Call 和状态切换

**原则**：每个 Draw Call 都有 CPU 驱动开销（状态验证、命令转换）。合并提交减少开销。

**反模式**：每个 UI 元素单独触发一次绘制；不同纹理/着色器/混合模式频繁切换。

**DDNet 现状**：CUI 的 `DoButton_Menu()`、`DoLabel()` 直接调用 `Graphics()->QuadsDrawTL()` 或 `TextRender()->RenderTextContainer()`，没有统一的批处理层。

**对应策略**：E（Section 内 Quad 批处理）

#### 维度 3：少画 — 减少 Overdraw 和像素填充

**原则**：每个像素被多次绘制浪费 GPU 填充率和带宽。

**反模式**：大面积半透明面板互相重叠。

**DDNet / QmUI 现状**：QmUI 的成本不能只看 `DrawCard()`。当前 `CUiRuntimeV2` 至少分成四段：

- tree begin/end：UI tree 的帧级构建与回收
- animation runtime：动画推进与状态解析
- render bridge：把 QmUI 结构提交到实际渲染桥
- card/widget draw：具体控件的背景、边框、高光与内容绘制

`DrawCard()` 的 4 层绘制确实带来 overdraw，但在 DDNet 这类 2D 桌面场景里，它更可能是**次级 GPU 成本**；QmUI 更需要先回答的是：当前慢的是 tree / anim / render bridge / 还是 widget draw。本轮文档默认把 QmUI 视为一个**独立 runtime 成本域**，而不是只把它当“卡片 overdraw 问题”。

**对应策略**：P2 优先级，低收益

#### 维度 4：少重建 — 缓存静态内容

**原则**：静态文本、布局、网格每帧重建白白消耗 CPU。

**反模式**：每帧重新创建文本容器；每帧重新计算布局。

**DDNet 现状**：CUI 的 immediate-mode 天然倾向每帧重建。`CUIElement` 提供 Quad 容器 + TextContainer 缓存，但覆盖率不足。很多地方仍用不缓存的 `DoLabel(const CUIRect*, ...)` 每帧创建临时 text container。

**对应策略**：A（文本缓存全覆盖）、F（布局脏检测）

#### 维度 5：少等待 — 异步化重活

**原则**：I/O、解码、GPU 上传不应阻塞主线程，尤其不应阻塞交互帧。

**反模式**：用户滚动时主线程集中合并 493 个 job 结果。

**DDNet 现状**：资源 Jobs 已有异步框架，但合并阶段（主线程消费 job 结果）没有按帧预算严格控制。IDLE_DRAIN 模式下后台请求预算过大，导致 backlog 膨胀。

**对应策略**：D（Job 合并帧预算限制）

#### 维度 6：少分配 — 避免每帧内存分配

**原则**：`malloc/free` 在游戏循环中导致不可预测的帧时间尖峰。

**反模式**：每帧创建临时 `std::vector`、`std::string`；回调 lambda 捕获大对象。

**DDNet 现状**：ImGui 式的 callback lambda 可能触发分配；`std::unordered_map` 的 rehash。这个维度的收益通常较小（几微秒级），在消灭了更大的瓶颈后再考虑。

**对应策略**：P3 优先级

### 4.3 优化决策树

面对任何 UI 性能问题，按这个顺序判断——这个顺序本身就是按"投入产出比"排的：

```
1. 能不做吗？        → 裁剪、虚拟化、跳过不可见区域（收益最大）
2. 能少做吗？        → 脏检测、缓存、只更新变化部分
3. 能做更快吗？      → 批处理、减少状态切换
4. 能异步做吗？      → 后台线程/Jobs 池、延迟提交
5. 能不分配吗？      → 对象池、预分配、零分配热路径
```

---

## 5. 当前瓶颈归因

### 5.1 8 个瓶颈点

| # | 瓶颈 | 影响 | 根因 | 当前缓解 |
|---|------|------|------|---------|
| **B1** | 首帧冷启动 | 50-200ms | 预热未命中或未覆盖；预热走 Compact 但首帧走 Full | 启动预热有但不可靠 |
| **B2** | Section 全量测量 | 每多一个 section ~0.5ms | `CSectionLoader::Process()` 遍历全部 section | 无 |
| **B3** | 视口外 section 仍执行 widget | 浪费 CPU 在不可见 section | section 只做了 GPU 端裁剪（scissor），CPU 端仍走完 | 无 |
| **B4** | 文本容器每帧重建 | 每次 ~0.1-0.5ms | `DoLabel` 每次调 `CreateTextContainer`；`CUIElement` 文本池使用不充分 | `SettingsTextElement()` 但有遗漏 |
| **B5** | Job 结果集中合并 | **6839ms 尖峰** | IDLE_DRAIN 后台预算过大（24），backlog 无上限 | 预算系统存在但不限制 backlog 膨胀 |
| **B6** | 皮肤列表全量遍历 | 100+ 皮肤 ~10-30ms | 每帧遍历全部皮肤项，只渲染可见行 | `BuildSettingsSkinListPlan()` 但不覆盖已有列表 |
| **B7** | Draw call 碎片化 | 每 50 widgets ~0.5-2ms | 每个 widget 独立提交，无批处理层 | 无 |
| **B8** | QmUI runtime 归因不足 | 当前无法判断 tree / anim / render bridge / widget draw 谁是主因 | `perf/ui_runtime` 还没有形成页面级、操作级统一口径；现有认知过度集中在 `DrawCard` | `QmRt.cpp` 已有局部 stage，但未形成完整验收口径 |

### 5.2 当前唯一基线（2026-06-10）

采集条件：1ms 采样阈值，64141 个 frame-time 样本，339649 条有效日志，覆盖设置页 + 服务器列表 + Tee 页的混合操作。这份数据是 **FBO 清理后唯一基线**，当前 spec 的所有问题排序与优化优先级都以它为准。它同时也说明：若后续需要更严格的 settings-only 验收，还应在此基线之上补固定场景 session，而不是回退到 pre-FBO 历史数据。

```
verdict: FAIL
p50: 1.326ms  p90: 2.014ms  p95: 2.420ms  p99: 12.906ms
max: 342.777ms  spike: 464 个  avg: 2.050ms
```

Top 归因：

| 排名 | 类型 | 页面 | 耗时 | 详情 |
|------|------|------|------|------|
| 1 | Work Drain | settings:tee | **6839.642ms** | merge 272 jobs, 1339 requested, 330 loaded |
| 2 | Work Drain | settings:tee | 3901.435ms | merge 495 jobs, 248 requested, 1419 loaded |
| 3 | Work Drain | settings:tee | 528.220ms | merge 0 jobs, 60 loaded |
| 4 | List Interaction | server_browser | 68.325ms | 1276 items, 24 visible, 1252 skipped |

**口径说明**：这里的 `p50/p95/p99/max` 来自 frame-time 样本；`Work Drain 5575.793ms` 是归因事件耗时，不等价于 frame-time `max`，但可用于解释尾部尖峰来源。

**关键结论**：`p99` 已经从历史基线的 `25.547ms` 降到 `12.906ms`，说明 FBO 清理后整体尾延迟有明显改善；但 `spike` 仍有 464 个，而且 `settings:tee / Work Drain` 出现了 **6839.642ms** 和 **3901.435ms** 的极端尖峰，说明真正的阻塞项仍然是 Tee 页资源 drain/backlog 收口失败，而不是 FBO 缺失本身。

### 5.3 QmUI 当前证据缺口

当前仓库并不是完全没有 QmUI perf 数据。`src/game/client/QmUi/QmRt.cpp` 已经记录了：

- `perf/ui_runtime tree_begin_frame`
- `perf/ui_runtime anim_advance`
- `perf/ui_runtime render_bridge_begin_frame`
- `perf/ui_runtime tree_end_frame`
- `perf/ui_runtime ui_runtime_total`

但这些数据还不够直接回答设置页优化问题，因为它们目前缺少：

- page / operation 维度的稳定关联
- `layout_ms` 的统一来源
- `active_anims`、`draw_calls`、`clip_pushes` 这类更接近用户可见成本的字段
- CUI / QmUI 混合页面里“谁在主导这帧”的统一归因

因此，QmUI 在本轮里不应再被归纳为单一的 `DrawCard overdraw`，而应作为“需要独立 telemetry 收口的 runtime 子系统”。
### 5.4 哪些是根因，哪些是表象

**根因**（优化这些能从根本上减少工作）：
1. 视口外 section 仍在执行 widget（维度 1：做了不该做的事）
2. 文本容器每帧重建（维度 4：没缓存该缓的）
3. 极端滚动后的历史 backlog 在 `IDLE_DRAIN` 阶段被继续放大（维度 5：异步完成 ≠ 主线程不爆）

**表象**（优化这些只能掩盖问题）：
1. FBO cache miss（真正的问题是为什么要重建，而不是为什么没缓存）
2. p99 高（真正的问题是尖峰帧是从哪来的）
3. 预热"未命中"（真正的问题是预热路径和渲染路径不一致）

---

## 6. 优化策略

> 每个策略包含：对应维度、现状（代码实际）、改法、量化字段（设计意图）、预期收益

### 6.1 策略 A：文本缓存全覆盖（对应维度 4）

**现状**：`CUIElement` 已内置 text container 缓存机制。`DoLabel(CUIElement::SUIElementRect&, ...)` 能复用 text container，只在文本内容变化时重建。但设置页中大量 `DoLabel` 调用仍走不缓存的 `DoLabel(const CUIRect*, ...)` 版本，每帧创建临时 text container。

```cpp
// 改前（每帧重建 text container）：
Ui()->DoLabel(&LabelRect, aLabelText, 10.0f, TEXTALIGN_ML);

// 改后（复用 text container，只在文本变化时重建）：
CUIElement &El = SettingsTextElement(SETTINGS_TCLIENT, TAB_GENERAL, "my_label");
Ui()->DoLabel(El.Rect(0), &LabelRect, aLabelText, 10.0f, TEXTALIGN_ML);
```

**量化字段**（C++ 侧待实现）：
```
text_new=N       # 本帧新创建的 text container 数
text_reused=N    # 本帧复用的 text container 数
```

**预期收益**：每 section 0.5-2ms。改动量小（~50 行/页面），风险低（已有基础设施）。

### 6.2 策略 B：视口外 section 跳过 widget（对应维度 1）

**现状**：`CScrollRegion` 做 scissor 裁剪（GPU 端），但 CPU 端仍走完所有 widget 函数。即使 section 完全在视口外，依然执行布局、文本容器创建、`DoButtonLogic`。

**改法**：在 section 级别增加可见性判断。如果 section 完全在视口外（加上预取边距），只保留高度信息，跳过所有 widget 调用。

```cpp
// 伪代码
if(!SectionRect.IsIntersecting(ViewportRect, PrefetchMargin))
{
    pSection->SkipRenderHeight = pSection->CachedHeight;
    return; // 不调任何 widget
}
```

**量化字段**（C++ 侧待实现）：
```
sections=N         # section 总数
sections_visible=N # 可见 section 数
```

**预期收益**：视口外 section 节省 80%+ CPU。多 section 页面（如 General）节省 5-15ms。改动量中等（~30 行/页面），风险中等（需确保高度测量正确）。

### 6.3 策略 C：列表虚拟化（对应维度 1）

**现状**：皮肤列表遍历全部项（`menus_settings.cpp` 的 `DoNextRow` 循环），即使只有 8-16 行可见。

**改法**：只遍历和渲染可见行 + 1-2 行预取缓冲。利用 `CScrollRegion` 的可见行范围。

**量化字段**（C++ 侧待实现）：
```
rows_total=N     # 列表总项数
rows_visible=N   # 可见行数
rows_rendered=N  # 实际渲染的行数
rows_skipped=N   # 跳过的行数
```

**预期收益**：含 100+ 项列表节省 8-20ms。改动量中等，风险中等（需确保高度计算和滚动位置正确）。

### 6.4 策略 D：Job 合并帧预算限制（对应维度 5）

**现状**：`IDLE_DRAIN` 模式后台请求预算为 24，且"健康进展"时允许继续扩大 backlog。可见区项本身仍然优先，但在一次大跨度滚动已经制造出大量历史 `BACKGROUND_REQUESTED` 后，这个预算会继续放大积压，最终在同一帧集中合并。

**改法**：
1. 将 `IDLE_DRAIN` 的后台请求预算从 24 降到 8（`settings_resource_jobs.cpp` 的 `SettingsSkinThroughputProfileForMode`）
2. 在 `SettingsSkinBackgroundRequestBudgetDecision` 中增加硬 backlog 上限：`HardBacklogLimit = max(CountFuseLimit, DefaultBudget * 8)`，超过即阻塞
3. 收紧"健康进展"的判断条件：必须 loaded 和 admitted 的 delta 都 ≤ 0 才允许继续扩大 backlog

**量化字段**（已有，需要收紧语义）：
```
event=work_drain page=settings:tee kind=merge count=N bytes=N dur_ms=N stop=pending|backpressure|budget
```

**预期收益**：消除 5575ms 级别的尖峰帧。改动量中等（集中在 `settings_resource_jobs.cpp`），风险：需要验证不会导致皮肤列表加载过慢。

### 6.5 策略 E：Section 内 Quad 批处理（对应维度 2）

**现状**：`DoButton_Menu()` 内部：`RenderQuadContainer`（背景）+ `RenderTextContainer`（文字）= 每次 2 draw calls。一个 section 10 个 button = 20 draw calls。

**改法**：收集 section 内可以安全合并的 quad 到 `CUIElement` 批处理容器，尽量减少提交次数，但**不能改变现有的 immediate-mode 语义**。硬约束如下：

- 不改变原始提交顺序
- 不跨 `clip/scissor` 区间重排
- 不跨 text/quad 层级边界重排
- 任何批处理都必须保持 hover、边框、背景、文字的可见结果一致

**量化字段**（C++ 侧待实现）：
```
draw_calls=N  # 本帧 draw call 数
```

**预期收益**：每 section 0.3-1ms。改动量中等（~100 行 helper），风险中等（需确保顺序和裁剪正确）。

### 6.6 策略 F：布局脏检测（对应维度 4）

**现状**：section 高度、列宽、可见范围等布局输入每帧重算。单个 `HMargin`/`VSplit*` 成本很低，但累积到几十个 section、上百个 widget 时不可忽略。

**改法**：缓存 section 高度、列表行高、列宽、可见范围和静态文本测量结果。dirty key 至少包含：viewport 宽高、UI scale、语言/文本、字体大小、配置展开状态。不缓存每个普通 rect split 的中间结果（维护成本 > 收益）。

**量化字段**（C++ 侧待实现）：
```
layout_dirty=0/1          # 本帧是否重算了布局
dirty_reason=width_changed|lang_changed|config_changed|...
```

**预期收益**：多 section/大列表页收益明显。改动量中等（按 section），风险中等（dirty key 必须完整，否则会出现 stale 布局）。

### 6.7 策略优先级总结

| 优先级 | 策略 | 维度 | 改动量 | 预期收益 | 风险 | 量化字段 |
|--------|------|------|--------|---------|------|----------|
| **P0** | A: 文本缓存全覆盖 | 4 - 少重建 | 小 | 每 section 0.5-2ms | 低 | `text_new` `text_reused` |
| **P0** | B: 视口外 section 跳过 | 1 - 少做 | 中 | 视口外 section 80%+ | 中 | `sections` `sections_visible` |
| **P0** | D: Job 合并预算限制 | 5 - 少等待 | 中 | 消除 5575ms 尖峰 | 中 | `work_drain` stop reason |
| **P1** | C: 列表虚拟化 | 1 - 少做 | 中 | 含 100+ 项列表 8-20ms | 中 | `rows_total` `rows_visible` `rows_skipped` |
| **P1** | F: 布局脏检测 | 4 - 少重建 | 中 | 多 section 页明显 | 中 | `layout_dirty` `dirty_reason` |
| **P2** | E: Quad 批处理 | 2 - 少提交 | 中 | 每 section 0.3-1ms | 中 | `draw_calls` |

---

## 7. 量化系统设计

> 本章描述的是量化系统的**设计意图**，不是进度报告。已实现的部分标注了"✅"，待实现的部分是设计目标。

### 7.1 设计目标

量化系统回答三个问题：
1. **哪里慢？** — 把一帧的耗时归因到具体的 stage（页面切换、列表处理、资源 drain、section 渲染）
2. **有多慢？** — p50/p90/p95/p99 百分位 + 尖峰帧检测 + 超标判定
3. **比以前好了还是差了？** — 同路径同环境的前后对比

两个层次的指标不能互相替代：
- **内部 perf 字段**（`duration_ms`、`stage`、`page`）：归因用，回答"哪里慢"
- **无上限 FPS**（`avg/max/min FPS`）：体感用，回答"用户觉得快了吗"

### 7.2 采集层（C++ 侧）

#### 阈值

当前客户端使用 **1ms** 作为默认采样阈值（`qm_perf_debug_threshold_ms 1`）。只有耗时超过阈值的 `LogPerfStage` 调用才会写入日志。阈值越低，捕获的长帧越完整，但日志量越大。

设计建议：
- 开发/自测期间：1ms（捕获所有超出 UI 预算的帧）
- 用户反馈收集：4ms（平衡信息量和日志量）
- 不应写死为固定值，保留配置项灵活性

#### 结构化字段

需要新增的最小字段集（按优先级排序）：

**P0 — 阻塞了最有价值的归因分析：**

```
# 页面切换事件
event=page_switch page=... from=... to=... dur_ms=...

# 列表帧事件
event=list_frame page=... items_total=... rows_visible=... rows_rendered=... rows_iterated=... rows_skipped=...

# Section 渲染事件
event=section page=... section=... dur_ms=... visible=0/1 dirty=... text_new=... text_reused=...

# 工作排空事件
event=work_drain page=... kind=merge/finalize/upload/publish count=... bytes=... dur_ms=... stop=...

# 操作路径 FPS 摘要（不是每帧事件，是操作结束后输出一次）
event=fps_summary operation=... page=... fps_avg=... fps_max=... fps_min=... sample_frames=... sample_seconds=... cap_limited=0/1
```

**P1 — QmUI 单独归因：**

```
event=ui_runtime page=... operation=... nodes=... layout_ms=... layout_dirty=0/1 dirty_reason=... anim_ms=... active_anims=... render_bridge_ms=... draw_calls=... clip_pushes=...
```

QmUI 走的是 `CUiRuntimeV2`（tree + layout engine + animation runtime + render bridge），不能把它的成本和 CUI 的 `DoLabel`/`DoButtonLogic` 混在同一个字段里。当前 `QmRt.cpp` 已经有局部 `perf/ui_runtime` stage，但还缺少 settings-only 验收真正需要的 page / operation / layout / animation / bridge 汇总字段。

#### 日志格式

保持当前 key=value 文本格式兼容旧工具。JSON Lines（`qm_perf_json 1`）作为后续增强。

#### 日志点位置

- 菜单级：`src/game/client/components/menus.cpp` 的 `LogPerfStage`
- 设置页级：`src/game/client/components/menus_settings.cpp:103-114` 的 `LogPerfStage`
- Section 级：在 `CSectionLoader::Process()` 中新增（待实现）
- 资源级：`src/game/client/components/settings_resource_jobs.cpp` 已有 `perf/settings-resource`

### 7.3 分析层（TS 侧）✅ 已实现

**文件**：`qmclient_scripts/perf/`

```
qmclient_scripts/perf/
├── package.json          # tsx + @types/node + typescript
├── tsconfig.json         # ES2022 + ESM
├── analyze.ts            # CLI 入口（自动发现最新日志 → Perf_Report/ 输出）
├── lib/
│   ├── parse.ts          # key=value + JSON Lines 双格式解析
│   ├── stats.ts          # 16 个导出：百分位/合规率/KDE/箱线图/QQ图/snapshot/会话对比
│   └── report.ts         # 论文式 HTML 模板（莫兰迪色系，7 节 12 图）
└── test/                 # 预留
```

**已实现的统计维度：**

| 统计项 | 方法 | 输出 |
|--------|------|------|
| 帧时间趋势 | 时间序列 | Timeline 折线图（对数 Y 轴）+ 三条阈值线 |
| 帧时间分布 | 分桶 + Gaussian KDE | 直方图 + 核密度估计叠加 |
| 百分位概览 | 排序取 p50/p90/p95/p99 | 垂直柱状图 |
| 分布诊断 | QQ Plot vs 正态分布 | 散点图 + 参考线 |
| 页面耗时分解 | 按页面分组的 Box Plot | 箱线图（Tukey, 1.5×IQR） |
| 尖峰帧列表 | filter dur > 16.67ms | 表格（时间/Stage/Page/耗时/超标倍数） |
| Session 自动对比 | 自动找上一次日志 | Delta 卡片矩阵 |
| 采样偏差检测 | p5 推断阈值 | 警告横幅 |
| 归因排序 | 按 kind 聚合 | Top-10 归因表 |

**依赖 C++ 数据增强后才可完整实现的统计：**
- Section 耗时排行的完整生命周期视图（当前已有 `perf/section`，但覆盖仍不完整）
- QmUI runtime 分解（当前已有局部 `perf/ui_runtime` stage，但还需要与页面/操作路径关联并补齐 `layout_ms`、`active_anims`、`draw_calls` 等字段）
- 文本缓存效率的全页面对比（当前已有 `text_new`/`text_reused`，但覆盖仍不完整）
- 列表虚拟化效率（需要 `rows_*` 字段）

### 7.4 报表层（ECharts HTML）✅ 已实现

自包含 HTML 报表，浏览器直接打开。特点：

- **莫兰迪色系**（低饱和度、论文式排版）
- **对数 Y 轴**：正常帧（1-4ms）和尖峰帧（100ms+）在同一张图里都清晰可见
- **三条帧预算标线**：4.17ms (240Hz) / 8.33ms (120Hz) / 16.67ms (60Hz)
- **dataZoom**：时间轴可拖拽缩放
- **采样偏差自动检测**：p5 > 4.17ms 时自动警告"当前阈值可能遗漏灰色尖峰"
- **Session 自动对比**：自动找上一次日志，逐指标计算 delta 方向和变化百分比

**关键设计决策：**

- **为什么不用 Python？** TS + `tsx` 只需 `npm install`，DDNet 已大量使用 JS/TS 生态
- **为什么 ECharts 而不是 Chart.js？** `dataZoom` 组件对时间序列体验极佳，内置 boxplot
- **为什么 HTML 而不是 PNG？** 可交互（缩放、tooltip），无需 headless 浏览器依赖
- **为什么 Timeline 用对数 Y 轴？** 性能数据有极端离群值（正常 1ms vs 尖峰 5575ms），线性 Y 轴下尖峰撑满高度，基线不可读

### 7.5 当前实现状态

| 层 | 状态 | 说明 |
|----|------|------|
| TS 脚本（解析+统计+报表） | ✅ 已完成 | 超越原始设计（从 5 种图表扩展到 12 种） |
| C++ 日志框架 | ✅ 已有 | `LogPerfStage` 可用，阈值可配置 |
| C++ `perf/section` 日志点 | ⚠️ 部分实现 | 已有事件，但当前仍是局部包装采样，不是 `CSectionLoader::Process()` 的完整生命周期视图 |
| C++ `text_new`/`text_reused` 字段 | ⚠️ 部分实现 | 已有字段，但当前覆盖仍不完整，尚不能做全页面缓存效率验收 |
| C++ `rows_*` 字段 | ❌ 待实现 | 阻塞了列表虚拟化效率验证 |
| C++ `perf/ui_runtime` 摘要 | ⚠️ 部分实现 | `QmRt.cpp` 已有 tree / anim / render bridge / total stage，但还缺 page / operation / layout / animation 数量等验收字段 |
| C++ `fps_summary` 事件 | ❌ 待实现 | 阻塞了端到端 FPS 对比 |
| CI 集成 | ❌ 待实现 | 阻塞了自动化回归防护 |
| 测试覆盖 | ❌ 待实现 | 边界样本测试（空日志、单样本、缺字段） |

---

## 8. 实施路线

### 8.1 Phase 0：实现前 UI Bug Audit

在动任何性能代码之前，先确认要改的路径上没有已知 bug：

1. 列出本轮要动的页面、tab、列表、滚动、输入路径
2. 搜索已有 bug 报告：花屏、错位、stale 内容、焦点丢失、输入吞掉、滚动跳动、选择错误
3. 重点复查上一轮性能优化（FBO、预热、缓存、异步 jobs）的副作用
4. 如果发现影响同路径的 bug，先修 bug → 再优化

### 8.2 Phase 1：源头降温

按优先级降序：

**P0（最高优先）：**

| 任务 | 策略 | 目标瓶颈 | 预期收益 |
|------|------|---------|---------|
| 补全所有 section 的静态文本缓存 | A | B4 | 每 section 节省 0.5-2ms |
| Section 测量延迟化：仅在已有稳定高度占位或测量缓存时才缩到预取范围 | B | B2 | 多 section 页节省 5-15ms |
| 视口外 section 跳过 widget 调用 | B | B3 | 视口外 section 节省 80%+ |
| IDLE_DRAIN 预算降到 8 + 硬 backlog 上限 | D | B5 | 消除 5575ms 级尖峰 |

**P1（高优先）：**

| 任务 | 策略 | 目标瓶颈 | 预期收益 |
|------|------|---------|---------|
| 皮肤列表虚拟化：只遍历可见行 | C | B6 | 100+ 项列表节省 8-20ms |
| 布局脏检测 + 缓存 section 高度 | F | B4 | 多 section 页收益明显 |
| QmUI runtime telemetry 分离与补全 | — | B8 | 先把 QmUI 从“卡片 overdraw 猜测”提升为可验收的独立 runtime 归因 |

**P2（中优先）：**

| 任务 | 策略 | 目标瓶颈 | 预期收益 |
|------|------|---------|---------|
| Section 内 Quad 批处理 | E | B7 | 每 section 节省 0.3-1ms |
| 减少非交互元素的 `DoButtonLogic` | — | B7 | 每 section 0.1-0.3ms |

### 8.3 Phase 2：缓存（源头降温完成后）

只在源头降温做完、且 telemetry 证明某个 section 的实时 CPU 成本仍 > 0.5ms 时，才考虑补充**非 FBO 的 CPU 侧缓存**，例如：

- 文本容器缓存补全
- section 高度/测量结果缓存
- 列表可见范围与布局结果缓存
- 可复用的列表计划或轻量 page-local metadata

这里**不允许**重新引入任何 render target / recorded target / FBO / blit 路线。DDNet 是 2D 游戏，本轮缓存方向只允许减少 CPU 重建与重复测量，不允许回到纹理录制式页面缓存。

### 8.4 Phase 3：锦上添花

| 任务 | 预期收益 |
|------|---------|
| UI 图标合并纹理图集 | 减少 draw calls |
| Overdraw 量化分析 | 数据驱动决策 |
| 每帧内存分配审计 | 消灭分配尖峰 |

### 8.5 明确不做

- ❌ **FBO 扩展/保留/评估**：FBO 正在从项目中删除，不做任何 FBO 相关优化
- ❌ **全局 Draw List 批处理**：与 DDNet CUI 架构冲突，改动量巨大。Section 内批处理（策略 E）已够用
- ❌ **引入 retained-mode UI 框架**：与 DDNet 架构冲突
- ❌ **Server Browser / Demo Browser 优化**：等设置页做完、有 perf 证据后再考虑
- ❌ **全局 scheduler 大重构**：不改 DDNet upstream 架构

---

## 9. 附录

### 附录 A：FBO 删除后唯一基线（2026-06-10）

来源：`C:\Users\11054\AppData\Roaming\DDNet\dumps\QmClient_Perf\Perf_Report\qm_perf_2026-06-10_14-17-59_report.html`

说明：这份报告是 **FBO 清理后唯一基线**。虽然它仍包含混合操作路径，但当前所有 post-FBO 性能判断、计划分期和后续回归比较都应优先以它为准。若后续补跑 settings-only 固定场景，它们的角色是“更严格的验收样本”，不是替换这份 post-FBO 基线的历史地位。

```
采样阈值：~1ms（实际 p5 = 0.466ms）
样本帧数：64,141
总日志行：339,649
无效行：0
偏差：无（p5 < 4.17ms，数据完整）

百分位分布：
  p10=1.034  p25=1.131  p50=1.326  p75=1.724
  p90=2.014  p95=2.420  p99=12.906  max=342.777
  avg=2.050  std=9.051  iqr=0.593

判定：FAIL（p99 已过线，但 spike 仍为 464）
尖峰帧：464 个（>16.67ms 的帧）
```

与 2026-06-09 pre-FBO 历史对照的对比：

| 指标 | 2026-06-09（pre-FBO-cleanup） | 2026-06-10（post-FBO-cleanup） |
|------|-------------------------------|-------------------------------|
| p50 | 1.147ms | 1.326ms |
| p95 | 4.804ms | 2.420ms |
| p99 | 25.547ms | 12.906ms |
| max(frame-time) | 373.77ms | 342.777ms |
| spike 数 | 552 | 464 |
| Top 1 work drain | 5575.793ms | 6839.642ms |
| FBO 状态 | 仍在旧历史中可见 | 设置页侧已清理，不再作为验收指标 |

解释：尾部整体明显改善，但最坏的 Tee 页 work drain 尖峰没有消失，甚至在这次混合 session 里更高。这说明 FBO 清理解决了旧路径的一部分总体噪声，但没有解决 Tee 页 backlog/drain 的根因。

### 附录 B：pre-FBO 历史对照（2026-06-09）

来源：`C:\Users\11054\AppData\Roaming\DDNet\dumps\QmClient_Perf\Perf_Report\qm_perf_2026-06-09_05-35-31_report.html`

说明：这份报告是 **FBO 清理前、且包含混合操作路径** 的历史对照样本。它只用于和 `2026-06-10` 的 post-FBO 唯一基线做前后比较，不再承担任何当前 baseline 角色。

### 附录 C：FBO 删除进展

FBO 删除是本仓库的既定方向，当前设置页侧清理已完成。本轮之后：

- 设置页不再保留页面级 runtime FBO cache
- `section_loader` 不再保留 recorded render target / static layer cache API
- `qm_settings_fbo_cache`、`qm_settings_fbo_cache_debug` 配置变量已删除
- 后续优化不再以“恢复 FBO 命中率”为目标，而是以帧时间、p95/p99、尖峰数、滚动恢复时间为目标

具体清理范围见 `docs/superpowers/plans/2026-06-10-设置页FBO用户可见问题清理实现计划.md`。

### 附录 D：DDNet 2D 特性对优化方向的影响

DDNet 作为 2D 俯视角 sprite 游戏的特性，决定了哪些"通用优化"不适用：

| 通用优化手段 | DDNet 中的适用性 | 原因 |
|-------------|-----------------|------|
| FBO 渲染到纹理 | ❌ 正在删除 | 2D 游戏无后处理/3D 渲染需求，FBO 创建+录制+blit 成本（1-3ms）常高于实时渲染（0.3-0.5ms） |
| 全局 Draw List 批处理 | ❌ 不做 | 与 upstream CUI 架构冲突，改动量巨大 |
| Overdraw 优化 | ⚠️ 低优先级 | 桌面 GPU fill-rate 充足，2D UI 的 overdraw 通常不是主瓶颈 |
| 纹理图集 | ✅ 可行 | 合并 UI 图标到图集减少 draw call |
| 异步 I/O | ✅ 已有基础 | 资源 Jobs 框架可用，需要收紧预算 |
| 对象池 | ✅ 可行 | 复用文本容器、列表行 UI 元素 |

### 附录 E：外部资料对照

| # | 结论 | 外部依据 | 对本仓库的含义 |
|---|------|----------|----------------|
| 1 | UI 优化核心是少提交、少切换、少重建。FBO 是高级技巧，不是基本策略。 | Dear ImGui 文档强调批处理、零分配、clipper 虚拟化 | 先做源头降温（P0），再考虑缓存（Phase 2） |
| 2 | Immediate-mode UI 的性能关键在于每帧只做 O(visible) 工作 | ImGui FAQ: "many widgets? you may benefit from using the clipper" | 列表虚拟化（P1）优先于 FBO |
| 3 | 批处理是减少 draw call 的最有效手段 | LearnOpenGL 批处理章节；ImGui `ImDrawList` 设计 | Section 内批处理（P2）可行 |
| 4 | 60fps = 16.67ms/帧，120fps = 8.33ms/帧。p99 比平均帧时间更能反映流畅度 | Wikipedia Frame Rate；游戏行业标准 | p99 ≤ 16ms 为目标 |
| 5 | FBO readback 是同步阻塞点，强制 GPU pipeline flush | Khronos OpenGL Wiki | 已删除 FBO 的另一个原因 |

---

## 修订历史

| 日期 | 变更 |
|------|------|
| 2026-06-10 | 初始版本。整合 `2026-06-02-设置页UI性能优化第一性原理分析.md` 和 `2026-06-02-设置页性能量化系统设计.md`，更新代码现状、最新 perf 数据、FBO 删除进展。原两份文档标记为 `superseded`。 |
