# 设置页性能优化总纲 Implementation Index

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement linked child plans task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 以 `docs/superpowers/specs/2026-06-10-设置页性能优化总纲.md` 为唯一主纲要，按 Phase 0 → Phase 1 → Phase 2 → Phase 3 推进设置页性能优化。

**Architecture:** 本索引不直接实现代码，而是固定阶段顺序、子计划边界和验收口径。每个可执行改动必须落到一个独立 child plan；child plan 只能覆盖一个明确瓶颈，不能跨阶段混入 FBO、Server Browser、Demo Browser 或全局 scheduler 重构。

**Tech Stack:** C++, GoogleTest, CMake Windows gate, existing `qmclient_scripts/perf` telemetry, `docs/superpowers/specs/2026-06-10-设置页性能优化总纲.md`.

---

## Authoritative Spec

- Spec: `docs/superpowers/specs/2026-06-10-设置页性能优化总纲.md`
- Current post-FBO baseline: `2026-06-10 14:17:59`
- Baseline verdict: `FAIL`
- Baseline p99: `12.906ms`
- Baseline spikes: `464`
- First hotspot: `settings:tee / Work Drain`, `6839.642ms`, merge `272` job results
- Scope: Settings pages only; Server Browser and Demo Browser are explicitly deferred until settings-side work has evidence.

## Phase Map

| Phase | Spec meaning | Execution rule | Current child plan |
|-------|--------------|----------------|--------------------|
| Phase 0 | 实现前 UI Bug Audit | Every child plan starts with same-path UI bug audit before production edits. | Embedded as Task 0 in each child plan. |
| Phase 1 | 源头降温 | Do P0 first, then P1, then P2. Each row below gets its own child plan. | `docs/superpowers/plans/2026-06-10-设置页tee-work-drain源头降温实现计划.md` covers P0-D only. |
| Phase 2 | 非 FBO 的 CPU 侧缓存 | Start only after Phase 1 source cooling and telemetry proves residual real-time CPU cost. | Not started. |
| Phase 3 | 锦上添花 | Start only after Phase 1/2 evidence says core bottlenecks are resolved. | Not started. |

## Phase 1 Queue

Process this queue in order unless fresh telemetry changes the ordering.

| Order | Priority | Spec strategy | Child plan status | Notes |
|-------|----------|---------------|-------------------|-------|
| 1 | P0-D | Job 合并预算限制：`IDLE_DRAIN` 预算降到 8 + hard backlog cap | Ready: `docs/superpowers/plans/2026-06-10-设置页tee-work-drain源头降温实现计划.md` | Current first hotspot. |
| 2 | P0-A | 文本缓存全覆盖 | Needs child plan | Use `text_new` / `text_reused` telemetry to choose exact sections. |
| 3 | P0-B1 | Section 测量延迟化 | Needs child plan | Keep height stability and scroll correctness as primary risks. |
| 4 | P0-B2 | 视口外 section 跳过 widget 调用 | Needs child plan | Must preserve visible layout, focus, hover, and scroll positions. |
| 5 | P1-C | 皮肤列表虚拟化：只遍历可见行 | Needs child plan | Requires `rows_*` telemetry and selection/scroll contract tests. |
| 6 | P1-F | 布局脏检测 + 缓存 section 高度 | Needs child plan | Dirty key must include viewport, UI scale, language/font, and config state. |
| 7 | P1-QmUI | QmUI runtime telemetry 分离与补全 | Needs child plan | Make QmUI page/operation attribution measurable before optimizing QmUI internals. |
| 8 | P2-E | Section 内 Quad 批处理 | Needs child plan | Do not cross clip/scissor or text/quad ordering boundaries. |
| 9 | P2 | 减少非交互元素的 `DoButtonLogic` | Needs child plan | Only after telemetry proves it matters. |

## Child Plan Requirements

Every child plan must include:

- A `Spec Phase Mapping` section that states the exact spec phase and priority row it implements.
- A `Task 0: UI Bug Audit Before Performance Changes` section with the affected pages, tabs, scroll paths, and input paths.
- A baseline section using the current post-FBO baseline or a newer settings-only fixed scenario run.
- Red/green tests for the specific behavior being changed.
- Verification commands:

```powershell
qmclient_scripts/cmake-windows.cmd --build cmake-build-release --target run_cxx_tests -j 14
python qmclient_scripts/gate/check_gate.py --mode quick
```

If `check_gate --mode quick` only fails on the already documented unrelated `ruff format` findings in `qmclient_scripts/languages_qmclient/extract_strings.py` and `qmclient_scripts/languages_qmclient/generate_all.py`, record that as a gap and do not modify those scripts inside a settings performance child plan.

## Completion Criteria

This index is complete when:

1. Phase 1 P0-D has a child plan and is executed with verification evidence.
2. Phase 1 P0-A, P0-B1, and P0-B2 each have their own child plan before implementation.
3. Phase 2 does not start until Phase 1 source cooling evidence is recorded.
4. No child plan reintroduces FBO / render target / recorded target / blit caching.
5. Every completed child plan records whether fresh fixed-scenario perf data was collected for `PERF-SETTINGS-TEE-SWITCH` and `PERF-TEE-SCROLL`.
